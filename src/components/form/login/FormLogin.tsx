import { TheForm } from '..';

const FormLogin = () => {
  const onSubmit = () => {};
  return (
    <TheForm onSubmit={onSubmit}>
      <p>login form</p>
    </TheForm>
  );
};

export default FormLogin;
